{#-
11ty Pagination: https://www.11ty.dev/docs/pagination/

pagenation.pageNumber: 現在のページ番号(0始まり)
pagenation.pages.length: 総ページ数
pagenation.hrefs: 各ページのURL配列
pagenation.href.first: 最初のページURL
pagenation.href.previous: 前のページURL
pagenation.href.next: 次のページURL
pagenation.href.last: 最後のページURL

【常に3ページ表示のパターン】
<< < [1] 2  3  > >>
<< <  1 [2] 3  > >>
<< <  2 [3] 4  > >>
<< <  3 [4] 5  > >>
<< <  4 [5] 6  > >>
<< <  4  5 [6]  > >>
-#}
{%- if pagination.pages.length > 1 %}
  <ol class="c-pager mx-auto flex w-fit gap-2">
    <li><a {% if page.url !== pagination.href.first %}href="{{ pagination.href.first }}" class="underline hover:no-underline" aria-label="最初のページ"{% else %}class="no-underline pointer-events-none" aria-hidden="true"{% endif %}>&lt;&lt;</a></li>
    <li><a {% if pagination.href.previous %}href="{{ pagination.href.previous }}" class="underline hover:no-underline" aria-label="前のページ"{% else %}class="no-underline pointer-events-none" aria-hidden="true"{% endif %}>&lt;</a></li>
    {%- for pageEntry in pagination.pages %}
      {% if pagination.pageNumber === 0 or pagination.pageNumber === pagination.pages.length - 1 %}
        {%-
          if loop.index0 === pagination.pageNumber - 2
          or loop.index0 === pagination.pageNumber - 1
          or loop.index0 === pagination.pageNumber
          or loop.index0 === pagination.pageNumber + 1
          or loop.index0 === pagination.pageNumber + 2
        %}
          <li><a href="{{ pagination.hrefs[loop.index0] }}" {%- if page.url === pagination.hrefs[loop.index0] %}aria-current="page" class="no-underline pointer-events-none"{% else %}class="underline hover:no-underline"{% endif %}>{{ loop.index }}</a></li>
        {%- endif %}
      {% else %}
        {%-
          if loop.index0 === pagination.pageNumber - 1
          or loop.index0 === pagination.pageNumber
          or loop.index0 === pagination.pageNumber + 1
        %}
          <li><a href="{{ pagination.hrefs[loop.index0] }}" {%- if page.url === pagination.hrefs[loop.index0] %}aria-current="page" class="no-underline pointer-events-none"{% else %}class="underline hover:no-underline"{% endif %}>{{ loop.index }}</a></li>
        {%- endif %}
      {%- endif %}
    {%- endfor %}
    <li><a {% if pagination.href.next %}href="{{ pagination.href.next }}" class="underline hover:no-underline" aria-label="次のページ"{% else %}class="no-underline pointer-events-none" aria-hidden="true"{% endif %}>&gt;</a></li>
    <li><a {% if page.url !== pagination.href.last %}href="{{ pagination.href.last }}" class="underline hover:no-underline" aria-label="最後のページ"{% else %}class="no-underline pointer-events-none" aria-hidden="true"{% endif %}>&gt;&gt;</a></li>
  </ol>
{%- endif %}
{#-
参考：【...を使うパターン】
■5ページ以内
<< < [1] 2  3  4  5 > >>

■6ページ以上
<< < [1] 2  ... 6 > >>
<< <  1 [2] 3 ... 6 > >>
<< <  1  2 [3] 4 ... 6 > >>
<< <  1 ... 3 [4] 5  6 > >>
<< <  1 ... 4 [5] 6 > >>
<< <  1 ... 5 [6] > >>

{%- if pagination.pages.length > 2 and pagination.pages.length <= 5 %}
  <ol class="c-pager mx-auto flex w-fit gap-2">
    <li><a {% if pagination.href.previous %}href="{{ pagination.href.previous }}" class="underline hover:no-underline" aria-label="前のページ"{% else %}class="no-underline pointer-events-none" aria-hidden="true"{% endif %}>&lt;</a></li>
    <li><a {% if page.url !== pagination.href.first %}href="{{ pagination.href.first }}" class="underline hover:no-underline" aria-label="最初のページ"{% else %}class="no-underline pointer-events-none" aria-hidden="true"{% endif %}>&lt;&lt;</a></li>
    {%- for pageEntry in pagination.pages %}
      <li><a href="{{ pagination.hrefs[loop.index0] }}" {%- if page.url === pagination.hrefs[loop.index0] %}aria-current="page" class="no-underline pointer-events-none"{% else %}class="underline hover:no-underline"{% endif %}>{{ loop.index }}</a></li>
    {%- endfor %}
    <li><a {% if page.url !== pagination.href.last %}href="{{ pagination.href.last }}" class="underline hover:no-underline" aria-label="最後のページ"{% else %}class="no-underline pointer-events-none" aria-hidden="true"{% endif %}>&gt;&gt;</a></li>
    <li><a {% if pagination.href.next %}href="{{ pagination.href.next }}" class="underline hover:no-underline" aria-label="次のページ"{% else %}class="no-underline pointer-events-none" aria-hidden="true"{% endif %}>&gt;</a></li>
  </ol>
{%- elif pagination.pages.length >  %}
  <ol class="c-pager mx-auto flex w-fit gap-2">
    <li><a {% if page.url !== pagination.href.first %}href="{{ pagination.href.first }}" class="underline hover:no-underline" aria-label="最初のページ"{% else %}class="no-underline pointer-events-none" aria-hidden="true"{% endif %}>&lt;&lt;</a></li>
    <li><a {% if pagination.href.previous %}href="{{ pagination.href.previous }}" class="underline hover:no-underline" aria-label="前のページ"{% else %}class="no-underline pointer-events-none" aria-hidden="true"{% endif %}>&lt;</a></li>
    {%- for pageEntry in pagination.pages %}
      {%-
        if loop.index0 === 0
        or loop.index0 === pagination.pageNumber - 1
        or loop.index0 === pagination.pageNumber
        or loop.index0 === pagination.pageNumber + 1
        or loop.index0 === pagination.pages.length - 1
      %}
        <li><a href="{{ pagination.hrefs[loop.index0] }}" {%- if page.url === pagination.hrefs[loop.index0] %}aria-current="page" class="no-underline pointer-events-none"{% else %}class="underline hover:no-underline"{% endif %}>{{ loop.index }}</a></li>
      {%- elif loop.index0 === pagination.pageNumber - 2 or loop.index0 === pagination.pageNumber + 2 %}
        <li class="pointer-events-none">...</li>
      {%- endif %}
    {%- endfor %}
    <li><a {% if pagination.href.next %}href="{{ pagination.href.next }}" class="underline hover:no-underline" aria-label="次のページ"{% else %}class="no-underline pointer-events-none" aria-hidden="true"{% endif %}>&gt;</a></li>
    <li><a {% if page.url !== pagination.href.last %}href="{{ pagination.href.last }}" class="underline hover:no-underline" aria-label="最後のページ"{% else %}class="no-underline pointer-events-none" aria-hidden="true"{% endif %}>&gt;&gt;</a></li>
  </ol>
  {%- endif %}
#}
